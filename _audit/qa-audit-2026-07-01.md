# Aventario Website QA Audit — 2026-07-01

Full compatibility and functionality pass over the production site (`website/`, live at aventario.com), the Supabase backend, and the Vercel deployment. 105 in-scope pages. Forms tested against the live database; interactions tested in a real browser.

## Headline

- **Supabase is NOT deactivated.** The database is live and every real form writes to it (verified `201 Created` for `leads`, `webinar_registrations`, and the whitepaper gate, plus a full end-to-end contact-form submit). The "deactivated" feeling is most likely the lead-notification email, which I could not verify without database access (see M6).
- **Vercel is reconnected.** The Git link now points at `Aventario/aventario-website2026`. A push built a preview automatically (`target: null`, production untouched).
- The site is structurally healthy. The real defects are concentrated in the blog and the German mirror.

## What works (verified)

| Area | Result |
|---|---|
| Supabase project | Live. `auth/v1/health` 200. |
| Contact form (EN+DE) | End-to-end `POST /rest/v1/leads` → **201**, correct payload, success message shown. |
| Whitepaper gate | `POST /rest/v1/leads` (type=whitepaper) → **201**. |
| Webinar registration | `POST /rest/v1/webinar_registrations` → **201**. |
| Cookie consent (Consent Mode v2) | Banner on 101 pages. GA does not load before consent. Accept → granted, Decline → denied, both persist and hide. Correct. |
| Mobile menu | Works on index, all `services/*`, all `impact/*` (EN+DE), all `de/*`, and the blog indexes (55 pages). |
| Aventario AI toggle (new, `ai-toggle` branch) | Works both ways, no console errors. |
| Contact email / phone / address | Uniform across all pages (`office@aventario.com`, `+43 134 3354012`, Tuchlauben 7a). |
| Live site | Home, contact, `/de/` all return 200. |

## HIGH — broken, user-facing

**H1. Mobile menu is dead on all 40 English blog articles.** Every `blog/*.html` article (not the `blog.html` index) renders the hamburger button and the menu div but ships no toggle JS. On phones, blog articles have no working navigation. Confirmed live: clicking the button does nothing, no handler script present. Fix: add the same toggle IIFE the other pages use.

**H2. The German blog index is 40 dead links.** `de/blog.html` links to `/de/blog/<slug>` for 40 articles, but the `de/blog/` directory does not exist. Confirmed live 404 (`/de/blog/it-vendor-management`). Every article card on the German blog 404s. Fix: build the DE articles, or remove/redirect the German blog index until they exist.

**H3. Six service pages link to three impact pages that do not exist.** `impact/sodexo.html`, `impact/frankfurt-airport.html`, `impact/digital-transformation.html` are linked from the six service pages but were never created under the new `impact/` naming. Confirmed live 404 (`/impact/sodexo.html`). Fix: repoint to the correct existing slugs (e.g. `aviation-it-outsourcing-tender.html`, `pharma-*`).

**H4. "boutique consultancy" (retired term) appears on 11 blog pages.** In the author bio block and one body line. Confirmed live (2 hits on `blog/it-vendor-management.html`). Policy violation. Fix: replace with "Vienna-based consultancy".

## MEDIUM

**M1. The newsletter signup is fake on 93 pages.** Every footer "Subscribe" form runs `onsubmit="event.preventDefault(); …textContent='Subscribed'"` with no network call. Visitors who subscribe are captured nowhere. Either wire it to Supabase / the email tool, or remove it.

**M2. German webinar confirmation redirects to the English page.** `de/webinar.html` redirects to `/webinar-thanks` instead of `/de/webinar-thanks` (which exists). German registrants land on the English thank-you.

**M3. Two German resource PDFs 404.** `de/resources.html` links `/de/resources/Whitepaper-Standardization.pdf` and `/de/resources/Aventario-Masterclass-Onepager.pdf`; the files exist only at `/resources/`. Repoint the links.

**M4. The webinar registration CTA goes nowhere on two pages.** `resources/savings-standardization-webinar.html` (EN+DE) has `<a href="#" id="registerButton">Register Here</a>` with no handler (flagged in code as pending the EverWebinar URL). The primary CTA on those landing pages is dead.

**M5. Stale draft homepages are deployed at the site root.** `index-v2.html`, `index.previous-pre-v03.html`, `index.v03-draft.html` are publicly reachable and still load assets from external CDNs (Google Fonts, unpkg, cdn.tailwindcss.com), re-introducing the GDPR/self-host exposure the rest of the site removed. They also contain broken `contact.html#book` anchors. Remove them from the deploy root.

**M6. Lead-notification email — unverified, likely the "deactivated" symptom.** Leads land in the database, but if the pg_net → Resend notification trigger is off, you stop getting alerts and it feels dead. I could not check this without database access. I inserted 4 test rows during this audit; if the notification works you should have received emails for them. Needs the Supabase link (below) to confirm and to delete the test rows.

## LOW

- **L1.** German forms send the English `source` values (`contact.html`, `webinar.html`), so DE leads are indistinguishable from EN in the `source` column.
- **L2.** `portfolio.html` is an orphan with no footer and no legal links; `webinar-thanks.html` / `de/webinar-thanks.html` have no footer. Archive or complete.
- **L3.** No EN→DE language link. German pages link to EN, but English pages give no path to the German mirror.
- **L4.** The contact-page booking iframe URL exposes the internal M365 tenant name (`…@Kernperformance.onmicrosoft.com`). Cosmetic information disclosure.
- **L5.** A stale `.vercel/output/static/` build mirror (51 files) is committed in the repo. Prune it.

## Needs you

1. **Complete the Supabase OAuth link** (started in chat) so I can verify the lead-notification trigger and delete the 4 QA test rows I created (all marked `__qa_healthcheck__` / `__QA_AUDIT__`, emails `@aventario.test`).
2. **Tell me which fixes to apply.** H1, H3, H4, M2, M3, M5 are quick and I can do them now. H2 (German blog) and M1 (newsletter) are decisions, not just fixes.

## Deliverable

- Vercel connection: fixed. `ai-toggle` preview builds automatically.
- Shareable preview link (expires in ~23h): in chat.
